name: Keep Repo Alive
on:
  schedule:
    - cron: '6 0 * * *'  # 每天 UTC 0点运行（中国时间 8点）
  workflow_dispatch:     # 允许手动触发

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Keep repo active
        uses: actions/github-script@v7
        with:
          script: |
            console.log('Repo is alive!');
